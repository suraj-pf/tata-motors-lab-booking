import React, { useState, useEffect } from 'react'\nimport { Calendar, Clock, MapPin, Users, Edit, X, AlertTriangle, Loader2 } from 'lucide-react'\nimport { bookingsApi } from '../../api/bookings'\nimport { bookingRules } from '../../utils/bookingRules'\nimport toast from 'react-hot-toast'\n\nconst EditBookingModal = ({ booking, isOpen, onClose, onSubmit }) => {\n  const [formData, setFormData] = useState({\n    booking_date: '',\n    start_time: '',\n    end_time: '',\n    purpose: ''\n  })\n  const [errors, setErrors] = useState({})\n  const [isSubmitting, setIsSubmitting] = useState(false)\n  const [availableTimeSlots, setAvailableTimeSlots] = useState([])\n  const [availableEndTimes, setAvailableEndTimes] = useState([])\n\n  // Initialize form data when booking changes\n  useEffect(() => {\n    if (booking) {\n      setFormData({\n        booking_date: booking.booking_date,\n        start_time: booking.start_time,\n        end_time: booking.end_time,\n        purpose: booking.purpose || ''\n      })\n      // Generate time slots\n      generateTimeSlots(booking.booking_date, booking.start_time)\n    }\n  }, [booking])\n\n  // Generate available time slots\n  const generateTimeSlots = (date, currentStartTime = null) => {\n    if (!date) return\n    \n    const slots = []\n    const startHour = 6 // 06:30 AM start\n    const endHour = 17 // 05:30 PM end\n    \n    for (let hour = startHour; hour <= endHour; hour++) {\n      if (hour === 6) {\n        slots.push('06:30')\n      } else if (hour === 17) {\n        slots.push('17:30')\n      } else {\n        slots.push(`${hour.toString().padStart(2, '0')}:00`)\n      }\n    }\n    \n    setAvailableTimeSlots(slots)\n    \n    // Generate end times if start time is selected\n    if (currentStartTime) {\n      generateEndTimes(currentStartTime)\n    }\n  }\n\n  // Generate available end times based on start time\n  const generateEndTimes = (startTime) => {\n    if (!startTime) return\n    \n    const [startHour, startMin] = startTime.split(':').map(Number)\n    const startMinutes = startHour * 60 + startMin\n    \n    const slots = []\n    const endHour = 17 // 05:30 PM end\n    \n    // Generate slots from start time + 1 hour to 05:30 PM\n    for (let hour = startHour + 1; hour <= endHour; hour++) {\n      if (hour === 17) {\n        slots.push('17:30')\n      } else {\n        slots.push(`${hour.toString().padStart(2, '0')}:00`)\n      }\n    }\n    \n    setAvailableEndTimes(slots)\n  }\n\n  // Handle form changes\n  const handleChange = (e) => {\n    const { name, value } = e.target\n    \n    setFormData(prev => ({\n      ...prev,\n      [name]: value\n    }))\n    \n    // Clear errors for this field\n    if (errors[name]) {\n      setErrors(prev => ({\n        ...prev,\n        [name]: ''\n      }))\n    }\n    \n    // Handle date change\n    if (name === 'booking_date') {\n      generateTimeSlots(value)\n      setFormData(prev => ({\n        ...prev,\n        start_time: '',\n        end_time: ''\n      }))\n    }\n    \n    // Handle start time change\n    if (name === 'start_time') {\n      generateEndTimes(value)\n      setFormData(prev => ({\n        ...prev,\n        end_time: ''\n      }))\n    }\n  }\n\n  // Validate form\n  const validateForm = () => {\n    const newErrors = {}\n    \n    if (!formData.booking_date) {\n      newErrors.booking_date = 'Booking date is required'\n    }\n    \n    if (!formData.start_time) {\n      newErrors.start_time = 'Start time is required'\n    }\n    \n    if (!formData.end_time) {\n      newErrors.end_time = 'End time is required'\n    }\n    \n    if (!formData.purpose.trim()) {\n      newErrors.purpose = 'Purpose is required'\n    }\n    \n    // Validate time constraints\n    if (formData.start_time && formData.end_time) {\n      const timeValidation = bookingRules.validateTimeConstraints(\n        formData.start_time,\n        formData.end_time\n      )\n      \n      if (!timeValidation.isValid) {\n        newErrors.time = timeValidation.error\n      }\n      \n      // Validate current time constraints\n      const currentTimeValidation = bookingRules.validateCurrentTimeConstraints(\n        formData.booking_date,\n        formData.start_time\n      )\n      \n      if (!currentTimeValidation.isValid) {\n        newErrors.current_time = currentTimeValidation.error\n      }\n    }\n    \n    setErrors(newErrors)\n    return Object.keys(newErrors).length === 0\n  }\n\n  // Handle submit\n  const handleSubmit = async (e) => {\n    e.preventDefault()\n    \n    if (!validateForm()) {\n      toast.error('Please fix the errors in the form')\n      return\n    }\n    \n    setIsSubmitting(true)\n    \n    try {\n      await onSubmit(formData)\n    } catch (error) {\n      console.error('Edit booking error:', error)\n      \n      // Handle conflict errors\n      if (error.response?.data?.conflict) {\n        setErrors({\n          time: error.response.data.message\n        })\n      } else {\n        toast.error(error.response?.data?.error || 'Failed to update booking')\n      }\n    } finally {\n      setIsSubmitting(false)\n    }\n  }\n\n  if (!isOpen || !booking) return null\n\n  return (\n    <div className=\"fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50 p-4\">\n      <div className=\"bg-white rounded-2xl max-w-2xl w-full shadow-2xl max-h-[90vh] overflow-y-auto\">\n        {/* Header */}\n        <div className=\"p-6 border-b border-gray-200\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <h2 className=\"text-2xl font-bold text-ocean mb-2\">Edit Booking</h2>\n              <p className=\"text-gray-600\">Update your booking details</p>\n            </div>\n            <button\n              onClick={onClose}\n              className=\"p-2 hover:bg-gray-100 rounded-lg transition-colors\"\n            >\n              <X size={20} className=\"text-gray-500\" />\n            </button>\n          </div>\n        </div>\n\n        {/* Current Booking Info */}\n        <div className=\"p-6 bg-gray-50 border-b border-gray-200\">\n          <h3 className=\"text-sm font-semibold text-gray-700 mb-3\">Current Booking</h3>\n          <div className=\"grid grid-cols-1 md:grid-cols-2 gap-4 text-sm\">\n            <div className=\"flex items-center gap-2\">\n              <MapPin size={16} className=\"text-gray-500\" />\n              <span className=\"text-gray-600\">Lab:</span>\n              <span className=\"font-medium text-gray-900\">{booking.lab_name}</span>\n            </div>\n            <div className=\"flex items-center gap-2\">\n              <Calendar size={16} className=\"text-gray-500\" />\n              <span className=\"text-gray-600\">Date:</span>\n              <span className=\"font-medium text-gray-900\">{booking.booking_date}</span>\n            </div>\n            <div className=\"flex items-center gap-2\">\n              <Clock size={16} className=\"text-gray-500\" />\n              <span className=\"text-gray-600\">Time:</span>\n              <span className=\"font-medium text-gray-900\">\n                {booking.start_time} - {booking.end_time}\n              </span>\n            </div>\n            <div className=\"flex items-center gap-2\">\n              <span className=\"text-gray-600\">Purpose:</span>\n              <span className=\"font-medium text-gray-900\">{booking.purpose || 'N/A'}</span>\n            </div>\n          </div>\n        </div>\n\n        {/* Edit Form */}\n        <form onSubmit={handleSubmit} className=\"p-6 space-y-4\">\n          {/* Date */}\n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n              Booking Date\n            </label>\n            <input\n              type=\"date\"\n              name=\"booking_date\"\n              value={formData.booking_date}\n              onChange={handleChange}\n              min={new Date().toISOString().split('T')[0]}\n              className={`w-full px-4 py-3 rounded-xl border-2 focus:outline-none focus:ring-4 transition-all ${\n                errors.booking_date\n                  ? 'border-red-500 focus:border-red-500 focus:ring-red-500/10'\n                  : 'border-gray-300 focus:border-teal focus:ring-teal/10'\n              }`}\n            />\n            {errors.booking_date && (\n              <p className=\"text-red-500 text-sm mt-1\">{errors.booking_date}</p>\n            )}\n          </div>\n\n          {/* Time Slots */}\n          <div className=\"grid grid-cols-2 gap-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Start Time\n              </label>\n              <select\n                name=\"start_time\"\n                value={formData.start_time}\n                onChange={handleChange}\n                className={`w-full px-4 py-3 rounded-xl border-2 focus:outline-none focus:ring-4 transition-all ${\n                  errors.start_time\n                    ? 'border-red-500 focus:border-red-500 focus:ring-red-500/10'\n                    : 'border-gray-300 focus:border-teal focus:ring-teal/10'\n                }`}\n              >\n                <option value=\"\">Select start time</option>\n                {availableTimeSlots.map(time => (\n                  <option key={time} value={time}>{time}</option>\n                ))}\n              </select>\n              {errors.start_time && (\n                <p className=\"text-red-500 text-sm mt-1\">{errors.start_time}</p>\n              )}\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                End Time\n              </label>\n              <select\n                name=\"end_time\"\n                value={formData.end_time}\n                onChange={handleChange}\n                disabled={!formData.start_time}\n                className={`w-full px-4 py-3 rounded-xl border-2 focus:outline-none focus:ring-4 transition-all ${\n                  errors.end_time\n                    ? 'border-red-500 focus:border-red-500 focus:ring-red-500/10'\n                    : 'border-gray-300 focus:border-teal focus:ring-teal/10'\n                } ${!formData.start_time ? 'bg-gray-100 cursor-not-allowed' : ''}`}\n              >\n                <option value=\"\">Select end time</option>\n                {availableEndTimes.map(time => (\n                  <option key={time} value={time}>{time}</option>\n                ))}\n              </select>\n              {errors.end_time && (\n                <p className=\"text-red-500 text-sm mt-1\">{errors.end_time}</p>\n              )}\n            </div>\n          </div>\n\n          {/* Time Validation Errors */}\n          {errors.time && (\n            <div className=\"bg-red-50 border border-red-200 rounded-lg p-3\">\n              <div className=\"flex items-center gap-2\">\n                <AlertTriangle size={16} className=\"text-red-600\" />\n                <p className=\"text-sm text-red-800\">{errors.time}</p>\n              </div>\n            </div>\n          )}\n\n          {errors.current_time && (\n            <div className=\"bg-amber-50 border border-amber-200 rounded-lg p-3\">\n              <div className=\"flex items-center gap-2\">\n                <AlertTriangle size={16} className=\"text-amber-600\" />\n                <p className=\"text-sm text-amber-800\">{errors.current_time}</p>\n              </div>\n            </div>\n          )}\n\n          {/* Purpose */}\n          <div>\n            <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n              Purpose\n            </label>\n            <textarea\n              name=\"purpose\"\n              value={formData.purpose}\n              onChange={handleChange}\n              rows={3}\n              placeholder=\"Enter the purpose of your booking...\"\n              className={`w-full px-4 py-3 rounded-xl border-2 focus:outline-none focus:ring-4 transition-all resize-none ${\n                errors.purpose\n                  ? 'border-red-500 focus:border-red-500 focus:ring-red-500/10'\n                  : 'border-gray-300 focus:border-teal focus:ring-teal/10'\n              }`}\n            />\n            {errors.purpose && (\n              <p className=\"text-red-500 text-sm mt-1\">{errors.purpose}</p>\n            )}\n          </div>\n\n          {/* Actions */}\n          <div className=\"flex gap-3 pt-4\">\n            <button\n              type=\"button\"\n              onClick={onClose}\n              className=\"flex-1 px-4 py-3 bg-gray-200 hover:bg-gray-300 text-gray-800 rounded-xl font-medium transition-colors\"\n            >\n              Cancel\n            </button>\n            <button\n              type=\"submit\"\n              disabled={isSubmitting}\n              className=\"flex-1 px-4 py-3 bg-teal hover:bg-teal/90 text-white rounded-xl font-medium transition-colors flex items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed\"\n            >\n              {isSubmitting ? (\n                <>\n                  <Loader2 size={16} className=\"animate-spin\" />\n                  Updating...\n                </>\n              ) : (\n                <>\n                  <Edit size={16} />\n                  Update Booking\n                </>\n              )}\n            </button>\n          </div>\n        </form>\n      </div>\n    </div>\n  )\n}\n\nexport default EditBookingModal
